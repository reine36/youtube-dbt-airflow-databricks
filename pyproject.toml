[project]
name = "airflow-dbt-databrics-youtube"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "reine36", email = "reyalcantara55@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "dbt-core>=1.12.3",
]

[project.scripts]
airflow-dbt-databrics-youtube = "airflow_dbt_databrics_youtube:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
